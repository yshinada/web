# Memo

Password-protected single memo page.

## Password

The password is set in `script.js`.

```js
const PASSWORD = "papa";
```

## Notes

This is a static page. The memo is saved in the browser's `localStorage`, so it is stored per device and per browser.
